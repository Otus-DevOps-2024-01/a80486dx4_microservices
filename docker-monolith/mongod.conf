# Где и как храним данные
storage:
  dbPath: /var/lib/mongodb
  journal:
    enabled: true

# Куда пишем логи
systemLog:
  destination: file
  logAppend: true
  path: /var/log/mongodb/mongod.log

# Сетевой интерфейс
net:
  port: 27017
  bindIp: 127.0.0.1
